12:23 26.08.2012
  - Events#index -> Titel mit link versehen, so dass ohne JS die Events angezeigt werden können.
---


- Remove the catch-all route in config/routes.rb

- Attachments für die Events hinzufügen (Ausschreibungen, Ergebnisse)

- Email konfigurieren
  
  app/config/environments/{environment_name}.rb

    config.action_mailer.delivery_method = :smtp
    config.action_mailer.smtp_settings = {
      :address              => "smtp.gmail.com",
      :port                 => 587,
      :domain               => '[redacted]',
      :user_name            => '[redacted]',
      :password             => '[redacted]',
      :authentication       => 'plain',
      :enable_starttls_auto => true  }




